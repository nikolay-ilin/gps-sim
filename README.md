# gps-sim

Минимальная Python-заготовка для будущей работы с GPS/GNSS.

## Установка

```bash
cd gps-sim
python3 -m venv .venv
source .venv/bin/activate
pip install -e ".[dev]"
```

## Запуск

```bash
python -m gps_sim --help
gps-sim --help
```

## Тесты

```bash
pytest
```
